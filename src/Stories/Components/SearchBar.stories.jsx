import React from 'react';
import SearchBar from '../../Components/SearchBar';

export default {
  title: 'Search Bar',
  component: SearchBar,
};

export const Default = () => {
  return <SearchBar />;
};
